import React from 'react';

const Login = () => {
    return (
        <div>
            <h2>This is login</h2>
            <button className="btn btn-warning">Google sign in</button>
        </div>
    );
};

export default Login;